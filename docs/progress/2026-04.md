# Progress — April 2026

High-signal bullets only; narrative lives in git history.

- **CI:** Three-job pipeline (lint → test → integration); `make lint-docker`; kind node image digest-pinned on amd64.
- **Solve policy:** `-n` required with `-f`; YAML scan for `metadata.namespace` + cluster-scoped kinds; block `-f -`, URLs, `-k` / `kubectl kustomize`, tightened `taint` / `-A` handling.
- **Scenarios:** Ten cases (001–010); 007 initContainer; **008** ResourceQuota, **009** PVC/StorageClass, **010** NetworkPolicy egress; case-desk **SolveHints**; mock debrief / Senior copy extended.
- **REPL:** Shortcuts (`o`/`obs`, `t`, `x`, `l`/`logs`, `r`/`again`, `hist`); solve `r` repeats last kubectl; **`kubectl.Kube`** interface + **`fakeKube`** tests for **`observe`** / **`trace`**; **`help`** embedded from **`testdata/repl_help.golden`** ( **`POD_NOIR_UPDATE_REPL_HELP_GOLDEN=1`** ); **[AD-006](architecture-decisions.md)**.
- **Tests / DX:** Session loop tests (**accuse** HOT → **solve**, solve-mode **precinct**, **debrief** blocked vs success); **`kubectl` victory** tests (`stubKube`); **`make test-session`** (`./internal/session/...` only). **README** “First ~10 minutes” + **playtest-checklist** “Suggested first run” (**case-001**).
- **Docs:** **playtest-checklist** “If you’re stuck” ladder (case pick, **hint**/contacts, **accuse** iteration, **solve** hints, skills floor); **README** playtest blurb links it; **mock `accuse` on case-001** (HotHints / warm vs HOT).
- **Docs / tooling:** `AGENTS.md`, Cursor rules for README + progress sync, `podnoir version` / build metadata in Docker & Makefile.
- **Narrative:** Precinct intro + file-cabinet header + default briefing stub; README “training vs real kube”; Senior stub copy polish.
- **Governance:** **[docs/pod-noir-northstar.md](../pod-noir-northstar.md)** (accuracy + governance block); **[docs/architecture-decisions.md](../architecture-decisions.md)**; **`.cursor/rules/northstar-sync.mdc`**; mock debrief headers + per-case FolderTease/CurtainLine polish.
- **Roadmap:** Northstar §7 rewritten — **Current / Near term / Horizon** vs old MVP phases; links to AD + playtest checklist.
- **Contacts:** Four NPCs — **`hint`** = wire roster; **`hint senior`** / sysadmin / network / archivist for messages; per-scenario copy for all cases; **[AD-004](../architecture-decisions.md)**.
- **LLM wire:** HTTP **`ContactWire`** with static anchor + **`POD_NOIR_LLM_CONTACT_WIRE`**; **[AD-005](../architecture-decisions.md)**.
- **Playtest:** `make playtest-smoke` + **`make playtest-smoke-ci`** (parity with integration); **`make git-hooks`** (**`core.hooksPath=githooks`**, **pre-commit** + **pre-push**) + **`pre-commit-playtest.sh`**; **[docs/playtest-checklist.md](../playtest-checklist.md)** for representative manual passes.

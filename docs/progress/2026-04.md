# Progress — April 2026

High-signal bullets only; narrative lives in git history.

- **CI:** Three-job pipeline (lint → test → integration); `make lint-docker`; kind node image digest-pinned on amd64.
- **Solve policy:** `-n` required with `-f`; YAML scan for `metadata.namespace` + cluster-scoped kinds; block `-f -`, URLs, `-k` / `kubectl kustomize`, tightened `taint` / `-A` handling.
- **Scenarios:** Seven cases (001–007); 007 teaches failing **initContainer**; case-desk **SolveHints** on entering solve; mock debrief / Senior copy extended.
- **REPL:** Shortcuts (`o`/`obs`, `t`, `x`, `l`/`logs`, `r`/`again`, `hist`); solve `r` repeats last kubectl.
- **Docs / tooling:** `AGENTS.md`, Cursor rules for README + progress sync, `podnoir version` / build metadata in Docker & Makefile.
